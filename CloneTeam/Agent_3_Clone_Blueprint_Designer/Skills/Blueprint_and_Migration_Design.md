# Blueprint and Migration Design

Thiết kế cấu trúc team, trách nhiệm agent, KWSR, handoff, shared data, output và acceptance test. Với từng thành phần nguồn, chọn `Keep`, `Adapt`, `Drop` hoặc `Create`; ghi lý do, mapping đích, dependency, rủi ro và cách rollback.

